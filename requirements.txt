requests>=2.31
streamlit>=1.32
pandas>=2.0
openpyxl>=3.1
gspread>=6.0
google-auth>=2.28

# Opcional: renderizar catálogos armados con JavaScript (React/Vue/Angular).
# Gratis y de código abierto. Tras instalar, corre una sola vez:
#   playwright install chromium
playwright>=1.40
